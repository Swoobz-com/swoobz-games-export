node_modules/
dist/
vite-*.log
_*.mjs
shots*/
qa-boss/frames/
qa-boss/raw/
# decision-evidence renders (phase 236): contact sheets + crops built to let Tim rule by eye.
# 122MB / 313 files and fully regenerable from the raws + plates — the FINDINGS go in the
# handoff, the pixels stay local.
qa-boss/decisions/
# session-30 keying staging (phase 275): 2.4GB of frames + keyed PNGs + webm for the 35
# harvested clips. Same rule as above — fully regenerable by
#   node qa-boss/key-s30-harvest.mjs --char <id>
# from qa-boss/raw/ (itself re-downloadable via SESSION30-HARVEST-MANIFEST.tsv), so the
# FINDINGS go in SESSION30-GATE-REPORT.md and the pixels stay local.
qa-boss/staged-s30/

# editor/agent backups — never tracked (swept in once by a directory add, phase 137)
*.bak

# CHARACTER SHOWCASE OUTPUT (phase 302). 86MB of art + clips COPIED out of public/assets by
#   npx vite-node scripts/build-character-showcase.mjs
# Same rule as every other generated-pixel folder here: the BUILDER is tracked, the copies are not.
# Regenerate it any time; it derives the roster, the clip lists and the unlock gating from the
# shipped manifests, so it cannot drift from the game.
characters/
