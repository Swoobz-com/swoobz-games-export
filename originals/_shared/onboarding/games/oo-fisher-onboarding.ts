/** SHIM for the standalone runner. Minimal shape used by OoFisherExperience. */
export const ooFisherOnboarding = {
  gameId: 'oo-fisher',
  gameLabel: 'OO-Fisher',
  steps: [] as unknown[],
} as const
